<p id="notice"><%= notice %></p>

<h1>Listing Api V1 Filenames.Txts</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @api_v1_filenames.txts.each do |api_v1_filenames.txt| %>
      <tr>
        <td><%= link_to 'Show', api_v1_filenames.txt %></td>
        <td><%= link_to 'Edit', edit_api_v1_filenames.txt_path(api_v1_filenames.txt) %></td>
        <td><%= link_to 'Destroy', api_v1_filenames.txt, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Filenames.txt', new_api_v1_filenames.txt_path %>
